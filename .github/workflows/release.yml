# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: 74f860bbfdba766a793937ae0e858d9441177712554f031f2f7239ea2a414588
# extension-sha256: 831341d3ee3167c559aa83fd2eec5aea8adf94e6a5149c13b7c11c39ee3da874,13769e1644221b0364034cc9db6a352d14b85a9f4e35d0e111cfdabd22ba4c0c,41e40dad457efc53292249a211575505e9d6021d73d3fee2e52c4e9eede68eea,72b04805b3ea4a85b5a37bd769aa627a9079a173210ea63c9a022aaa07896921,2534fe8f4c6bd6fe93ad10e1348265961ece6542d3d71a6e566f8ffba4b66397
# input-sha256: 74c02575f7599f9e4a82cacc60f6d60692ee81dd1f7bec20441e6b375cf77cce,e7616b0c43cc0106779b5bab99ec9c1e5e8d30cbc51bff57a3698cb82454fdaa,5f8c6fcd0d569e17050f694571474faa0f7538d12a5d11cc51891cf01c59a53a,573400f8b12918fdb1129a00f20a0c42a5d7b2f5fbd606fd080b9178c1ac3ef2,d8502f59e36dd7708b727bc50509660b4dbabcfb4bdf47569f5bb1dcee8ef96a
name: bcf/release-authority-cutover
'on':
  workflow_dispatch: {}
permissions: {}
jobs:
  authorize:
    name: Authorize newest certified exact main without candidate checkout
    if: ${{ false }}
    permissions: {actions: read, contents: read}
    outputs: {subject_commit: '${{ steps.resolve.outputs.subject_commit }}', subject_tree: '${{ steps.resolve.outputs.subject_tree }}'}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 5
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Allocate private release authorization directories
      shell: bash
      run: |-
        set -euo pipefail
        mkdir -p -- '${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/certification' '${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/controller'
    - name: Resolve provider-authenticated release inputs
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/ea638478932a726782f6b10cd749044337e6878a/bin/bcf ci-github release resolve --repository "${{ github.repository }}" --output "${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/release-inputs.json"
      id: resolve
    - name: Download only the mechanically resolved certification artifact
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {artifact-ids: '${{ steps.resolve.outputs.certification_artifact_id }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ steps.resolve.outputs.certification_run_id }}', path: '${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/certification', digest-mismatch: error}
    - name: Download only the mechanically resolved controller artifact
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {artifact-ids: '${{ steps.resolve.outputs.controller_artifact_id }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ steps.resolve.outputs.controller_run_id }}', path: '${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/controller', digest-mismatch: error}
    - name: Authorize exact provider and Git custody
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/ea638478932a726782f6b10cd749044337e6878a/bin/bcf ci-github release authorize --repository "${{ github.repository }}" --bundle "${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/certification" --inputs "${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/release-inputs.json" --controller-wheel-dir "${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/controller" --output "${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}/release-authorization.json"
    - name: Upload immutable release authorization inputs
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with: {name: 'bcf-release-authorization-${{ github.run_id }}-${{ github.run_attempt }}', path: '${{ runner.temp }}/bcf-release-${{ github.run_id }}-${{ github.run_attempt }}', if-no-files-found: error, retention-days: 30}
  build:
    name: Build exact certified release bytes on a fresh hosted runner
    needs: [authorize]
    if: ${{ needs.authorize.result == 'success' }}
    permissions: {actions: read, contents: read}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
    - name: Check out only the authorized exact commit
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with: {ref: '${{ needs.authorize.outputs.subject_commit }}', fetch-depth: 0, persist-credentials: false}
    - name: Select the closed release interpreter
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: 3.12.14}
    - name: Select the exact real-compiler test Node runtime
      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
      with: {node-version: 22.23.2, package-manager-cache: false}
    - name: Download this attempt's authorization only
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-authorization-${{ github.run_id }}-${{ github.run_attempt }}', path: .artifacts/release-build/release-input}
    - name: Test and build the exact authorized release bundle
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" .github/scripts/build_release_bundle.py --output .artifacts/release-build --authorization .artifacts/release-build/release-input/release-authorization.json --artifact-name "bcf-release-build-${{ needs.authorize.outputs.subject_commit }}-${{ github.run_attempt }}"
    - name: Upload the exact untrusted build bundle
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with: {name: 'bcf-release-build-${{ needs.authorize.outputs.subject_commit }}-${{ github.run_attempt }}', path: .artifacts/release-build, if-no-files-found: error, retention-days: 30}
      if: ${{ always() }}
