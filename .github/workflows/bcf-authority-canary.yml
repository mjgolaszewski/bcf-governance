# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: 49afebf61f8d8514830e2c4315d879c9aa29fe8b03a17e8001b918c60959d434
# extension-sha256: 831341d3ee3167c559aa83fd2eec5aea8adf94e6a5149c13b7c11c39ee3da874,adde42bee237d4da4a67a72d6d5e8600e63ed010486cea4084391f940b9d1bb0,41e40dad457efc53292249a211575505e9d6021d73d3fee2e52c4e9eede68eea,72b04805b3ea4a85b5a37bd769aa627a9079a173210ea63c9a022aaa07896921,95b956f3cc565d0989552e0c964d45ae3bda021f2a01afb02b320aeb60379c48
# input-sha256: 74c02575f7599f9e4a82cacc60f6d60692ee81dd1f7bec20441e6b375cf77cce,e7616b0c43cc0106779b5bab99ec9c1e5e8d30cbc51bff57a3698cb82454fdaa,5f8c6fcd0d569e17050f694571474faa0f7538d12a5d11cc51891cf01c59a53a,b271126eff3e027388727b8324c113ef59024217bd5608b191850aeb0e09c8f7,ac33e3f0436768cf14ef7ccc40565c17b662ab738815581d54fb2bf3e9aad67b
name: bcf/authority-revocation-canary
'on':
  workflow_dispatch:
    inputs:
      scenario:
        description: Deterministic producer outcome for this exact run and every rerun attempt
        required: true
        default: success
        type: choice
        options: [success, producer-b-failure]
permissions: {}
jobs:
  admit:
    name: Admit isolated authority canary
    if: ${{ github.actor == 'mjgolaszewski' && github.ref == 'refs/heads/main' }}
    permissions: {actions: read, contents: read, statuses: write}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 2
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Authenticate admission and publish isolated pending authority
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/c3ddbd278baaed5e34a26872193b964828a09d64/bin/bcf ci-github canary admit --repository "${{ github.repository }}" --sha "${{ github.sha }}" --target-url "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
  producer-a:
    name: Execute canary producer A
    needs: [admit]
    if: ${{ needs.admit.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Check out the exact candidate commit
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with: {fetch-depth: 0, persist-credentials: false}
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Execute deterministic producer A
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" .github/scripts/authority_canary_producer.py --producer a --scenario "${{ inputs.scenario }}"
  producer-b:
    name: Execute canary producer B
    needs: [admit]
    if: ${{ needs.admit.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Check out the exact candidate commit
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with: {fetch-depth: 0, persist-credentials: false}
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Execute deterministic producer B
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" .github/scripts/authority_canary_producer.py --producer b --scenario "${{ inputs.scenario }}"
  observe:
    name: Observe canary precedence mechanically
    needs: [admit, producer-a, producer-b]
    if: ${{ always() && github.actor == 'mjgolaszewski' && github.ref == 'refs/heads/main' && needs.admit.result == 'success' }}
    permissions: {actions: read, contents: read, statuses: write}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 2
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Reconstruct exact-attempt jobs and publish isolated terminal authority
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/c3ddbd278baaed5e34a26872193b964828a09d64/bin/bcf ci-github canary observe --repository "${{ github.repository }}" --sha "${{ github.sha }}" --target-url "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
