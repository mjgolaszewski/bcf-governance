# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: 80e1ba94338c2305b33cd0aced9bebc0eb48c4d4b09f5fc8b32b25d4f2e9c542
# extension-sha256: e735825595374b594c7f40375a68208f3e8f031329ab9cdce6783db4a20e3d4a,04240211401c1bdec8988910f5e2f68946bbc982b9d3af9373ddfd1d5c9dfd41,44db0d6a184d599d5fdee4e6f4fe7e4318a8b3923b5121cba080a333c4c8da69,8ab2b0b2692f8c9c44f8a3fd42066789911e0bdf97413e6c92f5c708e53d204b
# input-sha256: f688a0c653baa033abe864610b56bdfef557e6c0e7f178cb3c5f40d136cad172,9bab9055ec8b52113a593fbc08c8931ea494a53f9af9e19b3eb3f3789c1efaa7,e22ff1bf31b25e4309b61e27f51158fc6fa8a29786d7367db3acddbc326de408,d6f09446e17bf15c63ba2d23f4a696c9daa5f675781bb6a1ba7fa8ed863976a4
name: bcf/authority-revocation-canary
'on':
  workflow_dispatch:
    inputs:
      scenario:
        description: Deterministic producer outcome for this exact run and every rerun attempt
        required: true
        default: success
        type: choice
        options: [success, producer-b-failure]
permissions: {}
jobs:
  admit:
    name: Admit isolated authority canary
    if: ${{ github.actor == 'mjgolaszewski' && github.ref == 'refs/heads/main' }}
    permissions: {actions: read, contents: read, statuses: write}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 2
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Authenticate admission and publish isolated pending authority
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/af037ecee3a006d6157923a7862eda653f011d54/bin/bcf ci-github canary admit --repository "${{ github.repository }}" --sha "${{ github.sha }}" --target-url "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
  producer-a:
    name: Execute canary producer A
    needs: [admit]
    if: ${{ needs.admit.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Check out the exact candidate commit
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with: {fetch-depth: 0, persist-credentials: false}
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Execute deterministic producer A
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" .github/scripts/authority_canary_producer.py --producer a --scenario "${{ inputs.scenario }}"
  producer-b:
    name: Execute canary producer B
    needs: [admit]
    if: ${{ needs.admit.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Check out the exact candidate commit
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      with: {fetch-depth: 0, persist-credentials: false}
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Execute deterministic producer B
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" .github/scripts/authority_canary_producer.py --producer b --scenario "${{ inputs.scenario }}"
  observe:
    name: Observe canary precedence mechanically
    needs: [admit, producer-a, producer-b]
    if: ${{ always() && github.actor == 'mjgolaszewski' && github.ref == 'refs/heads/main' && needs.admit.result == 'success' }}
    permissions: {actions: read, contents: read, statuses: write}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 2
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Reconstruct exact-attempt jobs and publish isolated terminal authority
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/af037ecee3a006d6157923a7862eda653f011d54/bin/bcf ci-github canary observe --repository "${{ github.repository }}" --sha "${{ github.sha }}" --target-url "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
