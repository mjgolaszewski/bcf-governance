# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: 9f43d5de2e700cf57ffb2b418afcc84a69f37dcda8ed7f58ec9255814835b946
# extension-sha256: f0ce7dbd17a655162acdc03e894983f072b6c864452180cb73deceea3a674022,04240211401c1bdec8988910f5e2f68946bbc982b9d3af9373ddfd1d5c9dfd41,44db0d6a184d599d5fdee4e6f4fe7e4318a8b3923b5121cba080a333c4c8da69,5910bbe104d1d527cc09acf67fba01235ff967a8ed1ea2641db2c73019f01d9f
# input-sha256: d9d78dc2ea8711929b46da008d35b081a2966c2c7d98115c97651efe77128e15,e22ff1bf31b25e4309b61e27f51158fc6fa8a29786d7367db3acddbc326de408,cebdf42cedb3e6fa35c0f95cfad2ce6d04d8a607d7b5e4c8a2d7d48d993f1357
name: bcf/pr-status-publisher
'on':
  workflow_run:
    workflows: [bcf/pr-finalizer]
    types: [completed]
permissions: {actions: read, checks: write, contents: read, pull-requests: read}
jobs:
  publish:
    name: Publish the authoritative exact-head PR certification
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions: {actions: read, checks: write, contents: read, pull-requests: read}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 5
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Download only the triggering finalizer observation
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-pr-finalization-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-pr-finalization-${{ github.run_id }}-${{ github.run_attempt }}', digest-mismatch: error}
    - name: Publish the sole exact-head PR certification check
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/af037ecee3a006d6157923a7862eda653f011d54/bin/bcf ci-github pr publish --repository "${{ github.repository }}" --bundle "${{ runner.temp }}/bcf-pr-finalization-${{ github.run_id }}-${{ github.run_attempt }}" --target-url "https://github.com/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}"
