# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: a7bb585786f9d79e5729fc92fbceb74e64743c938ae7ab36f802f230f0b18f6b
# extension-sha256: e735825595374b594c7f40375a68208f3e8f031329ab9cdce6783db4a20e3d4a,04240211401c1bdec8988910f5e2f68946bbc982b9d3af9373ddfd1d5c9dfd41,44db0d6a184d599d5fdee4e6f4fe7e4318a8b3923b5121cba080a333c4c8da69,8ab2b0b2692f8c9c44f8a3fd42066789911e0bdf97413e6c92f5c708e53d204b
# input-sha256: f688a0c653baa033abe864610b56bdfef557e6c0e7f178cb3c5f40d136cad172,9bab9055ec8b52113a593fbc08c8931ea494a53f9af9e19b3eb3f3789c1efaa7,e22ff1bf31b25e4309b61e27f51158fc6fa8a29786d7367db3acddbc326de408,e23ccc18c3f83474ec91f95f13dccdd3f0edc71622d98b7675dc1349e93ae92f
name: bcf/trusted-release-collector
'on':
  workflow_run:
    workflows: [bcf/release-byte-verifier]
    types: [completed]
permissions: {actions: read, contents: read}
jobs:
  collect:
    name: Recompute authenticated release evidence and emit the sole receipt
    if: ${{ false }}
    permissions: {actions: read, contents: read}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 10
    steps:
    - name: Provision the declared Python runtime
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: '3.12'}
    - name: Download only the triggering verifier artifact
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-verification-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}', digest-mismatch: error}
    - name: Recompute provider, Git, dependency, runtime, and asset custody
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/a247310f54e6a284cd2e6f591d4086d3c35e6184/bin/bcf ci-github release collect --repository "${{ github.repository }}" --bundle "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-input/certification" --authorization "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-input/release-authorization.json" --build-manifest "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-build-manifest.json" --verification "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/verification/release-verification.json" --runtime-report "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/runtime/runtime-verification.json" --runtime-evidence-dir "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/runtime" --release-artifact-dir "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/assets" --verification-artifact-name "bcf-release-verification-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}" --output "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/release-receipt.json" --bundle-output "${{ runner.temp }}/bcf-certified-release-${{ github.run_id }}-${{ github.run_attempt }}"
    - name: Upload the sole authoritative release receipt and exact assets
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with: {name: 'bcf-release-receipt-${{ github.run_id }}-${{ github.run_attempt }}', path: '${{ runner.temp }}/bcf-certified-release-${{ github.run_id }}-${{ github.run_attempt }}', if-no-files-found: error, retention-days: 90}
