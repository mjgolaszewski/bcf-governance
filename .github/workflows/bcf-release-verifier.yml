# Generated by BCF; edit governance/ci-graph.yml or a registered extension.
# graph-sha256: 5e3a7f67645c622c1eaf04733974e21d439d3ebcdcdf09a31a7649fb22080446
# extension-sha256: 831341d3ee3167c559aa83fd2eec5aea8adf94e6a5149c13b7c11c39ee3da874,13769e1644221b0364034cc9db6a352d14b85a9f4e35d0e111cfdabd22ba4c0c,41e40dad457efc53292249a211575505e9d6021d73d3fee2e52c4e9eede68eea,72b04805b3ea4a85b5a37bd769aa627a9079a173210ea63c9a022aaa07896921,2534fe8f4c6bd6fe93ad10e1348265961ece6542d3d71a6e566f8ffba4b66397
# input-sha256: 74c02575f7599f9e4a82cacc60f6d60692ee81dd1f7bec20441e6b375cf77cce,e7616b0c43cc0106779b5bab99ec9c1e5e8d30cbc51bff57a3698cb82454fdaa,5f8c6fcd0d569e17050f694571474faa0f7538d12a5d11cc51891cf01c59a53a,573400f8b12918fdb1129a00f20a0c42a5d7b2f5fbd606fd080b9178c1ac3ef2,e3876336a47c4ae6560f24f98d376e509f0aa7e30a452e6f0624f4eb5f24a2cf
name: bcf/release-byte-verifier
'on':
  workflow_run:
    workflows: [bcf/release-authority-cutover]
    types: [completed]
permissions: {}
jobs:
  runtime:
    name: Execute token-free offline wheel and sdist verification
    if: ${{ github.event.workflow_run.event == 'workflow_dispatch' && github.event.workflow_run.head_branch == 'main' && github.event.workflow_run.conclusion == 'success' }}
    permissions: {actions: read, contents: read}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
    - name: Select the closed release interpreter
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: 3.12.14}
    - name: Select the exact real-compiler test Node runtime
      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
      with: {node-version: 22.23.2, package-manager-cache: false}
    - name: Download the triggering run's trusted authorization and controller
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-authorization-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-release-authorization', digest-mismatch: error}
    - name: Install the triggering run's checksum-admitted controller
      shell: bash
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" -I -c 'import hashlib,json,pathlib,re,subprocess,sys,venv
        source=pathlib.Path('"'"'${{ runner.temp }}/bcf-release-authorization/controller'"'"')
        target=pathlib.Path('"'"'${{ runner.temp }}/bcf-controller'"'"')
        authority=pathlib.Path('"'"'${{ runner.temp }}/bcf-release-authorization/release-authorization.json'"'"')
        assert authority.is_file() and not authority.is_symlink()
        payload=json.loads(authority.read_text())
        keys=['"'"'controller'"'"', '"'"'wheel_sha256'"'"']
        expected=payload
        for key in keys:
         assert isinstance(expected,dict) and key in expected
         expected=expected[key]
        assert isinstance(expected,str) and re.fullmatch(r'"'"'[a-f0-9]{64}'"'"',expected)
        inventory=source/'"'"'SHA256SUMS'"'"'
        assert inventory.is_file() and not inventory.is_symlink()
        declared={}
        for line in inventory.read_text().splitlines():
         digest,separator,name=line.partition('"'"'  '"'"')
         assert separator and len(digest)==64 and name and name not in declared
         declared[name]=digest
        actual={path.name:path for path in source.iterdir() if path.name!='"'"'SHA256SUMS'"'"'}
        assert set(actual)==set(declared)
        assert all(path.is_file() and not path.is_symlink() and hashlib.sha256(path.read_bytes()).hexdigest()==declared[name] for name,path in actual.items())
        wheels=sorted(source.glob('"'"'bcf_governance-*.whl'"'"'))
        assert len(wheels)==1 and not wheels[0].is_symlink()
        assert hashlib.sha256(wheels[0].read_bytes()).hexdigest()==expected
        venv.EnvBuilder(with_pip=True,clear=True).create(target)
        subprocess.run([str(target/'"'"'bin/python'"'"'),'"'"'-m'"'"','"'"'pip'"'"','"'"'install'"'"','"'"'--no-index'"'"','"'"'--find-links'"'"',str(source),str(wheels[0])],check=True)
        subprocess.run([str(target/'"'"'bin/bcf'"'"'),'"'"'ci-github'"'"','"'"'--help'"'"'],check=True)
        '
    - name: Download only the triggering build artifact
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-build-${{ github.event.workflow_run.head_sha }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-release-build', digest-mismatch: error}
    - name: Execute package verification without provider credentials
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '', ACTIONS_ID_TOKEN_REQUEST_TOKEN: '', ACTIONS_RUNTIME_TOKEN: ''}
      run: |-
        set -euo pipefail
        "${{ runner.temp }}/bcf-controller/bin/bcf" ci-github release runtime --wheelhouse-manifest "${{ runner.temp }}/bcf-release-build/release/wheelhouse-manifest.yml" --lock "${{ runner.temp }}/bcf-release-build/release/requirements-cp312-linux-x86_64.lock" --wheelhouse "${{ runner.temp }}/bcf-release-build/wheelhouse" --release-artifact-dir "${{ runner.temp }}/bcf-release-build/assets" --python "$BCF_PYTHON" --output "${{ runner.temp }}/bcf-release-runtime"
    - name: Upload raw runtime evidence from this attempt
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with: {name: 'bcf-release-runtime-${{ github.run_id }}-${{ github.run_attempt }}', path: '${{ runner.temp }}/bcf-release-runtime', if-no-files-found: error, retention-days: 30}
      if: ${{ always() }}
  collect:
    name: Authenticate runtime and emit the sole trusted receipt
    needs: [runtime]
    if: ${{ needs.runtime.result == 'success' }}
    permissions: {actions: read, contents: read}
    runs-on: [self-hosted, Linux, X64, bcf-governance, vm-linux-ci-runner]
    timeout-minutes: 10
    steps:
    - name: Select the closed release interpreter
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
      with: {python-version: 3.12.14}
    - name: Download the triggering run's trusted authorization and controller
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-authorization-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-release-authorization-${{ github.run_id }}-${{ github.run_attempt }}', digest-mismatch: error}
    - name: Install the triggering run's checksum-admitted controller
      shell: bash
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python'}
      run: |-
        set -euo pipefail
        "$BCF_PYTHON" -I -c 'import hashlib,json,pathlib,re,subprocess,sys,venv
        source=pathlib.Path('"'"'${{ runner.temp }}/bcf-release-authorization-${{ github.run_id }}-${{ github.run_attempt }}/controller'"'"')
        target=pathlib.Path('"'"'${{ runner.temp }}/bcf-controller-${{ github.run_id }}-${{ github.run_attempt }}'"'"')
        authority=pathlib.Path('"'"'${{ runner.temp }}/bcf-release-authorization-${{ github.run_id }}-${{ github.run_attempt }}/release-authorization.json'"'"')
        assert authority.is_file() and not authority.is_symlink()
        payload=json.loads(authority.read_text())
        keys=['"'"'controller'"'"', '"'"'wheel_sha256'"'"']
        expected=payload
        for key in keys:
         assert isinstance(expected,dict) and key in expected
         expected=expected[key]
        assert isinstance(expected,str) and re.fullmatch(r'"'"'[a-f0-9]{64}'"'"',expected)
        inventory=source/'"'"'SHA256SUMS'"'"'
        assert inventory.is_file() and not inventory.is_symlink()
        declared={}
        for line in inventory.read_text().splitlines():
         digest,separator,name=line.partition('"'"'  '"'"')
         assert separator and len(digest)==64 and name and name not in declared
         declared[name]=digest
        actual={path.name:path for path in source.iterdir() if path.name!='"'"'SHA256SUMS'"'"'}
        assert set(actual)==set(declared)
        assert all(path.is_file() and not path.is_symlink() and hashlib.sha256(path.read_bytes()).hexdigest()==declared[name] for name,path in actual.items())
        wheels=sorted(source.glob('"'"'bcf_governance-*.whl'"'"'))
        assert len(wheels)==1 and not wheels[0].is_symlink()
        assert hashlib.sha256(wheels[0].read_bytes()).hexdigest()==expected
        venv.EnvBuilder(with_pip=True,clear=True).create(target)
        subprocess.run([str(target/'"'"'bin/python'"'"'),'"'"'-m'"'"','"'"'pip'"'"','"'"'install'"'"','"'"'--no-index'"'"','"'"'--find-links'"'"',str(source),str(wheels[0])],check=True)
        subprocess.run([str(target/'"'"'bin/bcf'"'"'),'"'"'ci-github'"'"','"'"'--help'"'"'],check=True)
        '
    - name: Download only the triggering build artifact
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-build-${{ github.event.workflow_run.head_sha }}-${{ github.event.workflow_run.run_attempt }}', github-token: '${{ github.token }}', repository: '${{ github.repository }}', run-id: '${{ github.event.workflow_run.id }}', path: '${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}', digest-mismatch: error}
    - name: Download only this verifier attempt's runtime evidence
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with: {name: 'bcf-release-runtime-${{ github.run_id }}-${{ github.run_attempt }}', path: '${{ runner.temp }}/bcf-release-runtime-${{ github.run_id }}-${{ github.run_attempt }}'}
    - name: Allocate private release verification directories
      shell: bash
      run: |-
        set -euo pipefail
        mkdir -p -- '${{ runner.temp }}/bcf-release-verification-${{ github.run_id }}-${{ github.run_attempt }}'
    - name: Authenticate provider state and recompute evidence hashes only
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "${{ runner.temp }}/bcf-controller-${{ github.run_id }}-${{ github.run_attempt }}/bin/bcf" ci-github release verify-evidence --repository "${{ github.repository }}" --authorization "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/release-input/release-authorization.json" --build-manifest "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/release-build-manifest.json" --wheelhouse-manifest "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/release/wheelhouse-manifest.yml" --lock "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/release/requirements-cp312-linux-x86_64.lock" --wheelhouse "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/wheelhouse" --release-artifact-dir "${{ runner.temp }}/bcf-release-build-${{ github.run_id }}-${{ github.run_attempt }}/assets" --runtime-report "${{ runner.temp }}/bcf-release-runtime-${{ github.run_id }}-${{ github.run_attempt }}/runtime-verification.json" --runtime-evidence-dir "${{ runner.temp }}/bcf-release-runtime-${{ github.run_id }}-${{ github.run_attempt }}" --output "${{ runner.temp }}/bcf-release-verification-${{ github.run_id }}-${{ github.run_attempt }}/release-verification.json" --bundle-output "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}"
    - name: Recompute provider, Git, dependency, runtime, and asset custody
      shell: bash
      working-directory: .
      env: {BCF_PYTHON: '${{ env.pythonLocation }}/bin/python', GITHUB_TOKEN: '${{ github.token }}'}
      run: |-
        set -euo pipefail
        "$RUNNER_TOOL_CACHE"/bcf-governance/f652538d6e5d9f509dda1b7c5d2001f8014b9a42/bin/bcf ci-github release collect --repository "${{ github.repository }}" --bundle "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-input/certification" --authorization "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-input/release-authorization.json" --build-manifest "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/release-build-manifest.json" --verification "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/verification/release-verification.json" --runtime-report "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/runtime/runtime-verification.json" --runtime-evidence-dir "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/runtime" --release-artifact-dir "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/build/assets" --verification-artifact-name "bcf-release-verification-${{ github.event.workflow_run.id }}-${{ github.event.workflow_run.run_attempt }}" --output "${{ runner.temp }}/bcf-release-collection-${{ github.run_id }}-${{ github.run_attempt }}/release-receipt.json" --bundle-output "${{ runner.temp }}/bcf-certified-release-${{ github.run_id }}-${{ github.run_attempt }}"
    - name: Upload the sole authoritative release receipt and exact assets
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with: {name: 'bcf-release-receipt-${{ github.run_id }}-${{ github.run_attempt }}', path: '${{ runner.temp }}/bcf-certified-release-${{ github.run_id }}-${{ github.run_attempt }}', if-no-files-found: error, retention-days: 90}
