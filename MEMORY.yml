document: {kind: durable_project_memory, name: BCF Governance Working Memory, id: bcf-governance-memory, version: 2.0.0, generated_at_utc: '2026-09-14T00:00:00Z', status: active, path: MEMORY.yml}
regression_shield:
  purpose: keep stable current decisions here and execution detail in governed phase artifacts
  invariants: [never record secrets or machine-local values, preserve historical release facts in audits and phase history, update canonical references at every phase rollover]
stable_decisions:
  canonical_repo_root: .
  canonical_governance_guide: AGENTS.yml
  canonical_product_spec: plans/product-spec.yml
  canonical_build_plan: plans/build-plan.yml
  canonical_active_phase_ledger: plans/phase-ledger.yml
  canonical_phase_history: plans/phase-history.yml
  canonical_phase_log_root: phases/
  code_roots: {backend: bcf_governance, frontend: docs}
  delivery_architecture: {backend: python_package_with_private_portable_runtime, frontend: canonical_operator_and_maintainer_guides, data: dependency_scoped_claim_receipts_and_content_addressed_evidence, observability: advisory_truth_metrics}
  governance_process: {semantic_validator: scripts/validate_governance_yaml.py, structural_schema_root: schemas/, hotfix_lane: governed_and_machine_readable, artifact_ownership: canonical_owner_map_recorded_in_AGENTS, phase_rollover: phase_ledger_memory_and_history_move_together, build_artifact_convention: follow_local_template_pack_shapes_and_naming}
memory_retention_policy:
  stable_decisions: {review_frequency: per_phase_rollover}
  environment_facts: {revalidate_after_days: 7}
  reusable_commands: {validate_on_release_gate: true}
  stale_entry_handling: [mark_deprecated_before_removal, preserve_replacement_reference_when_available]
environment_facts:
  current_repo_state:
  - P24 and immutable v1.3.0 are historical; plans/phase-history.yml and release custody audits own those facts.
  - P25 and its certified 2.0.0 baseline are historical; release_15 was superseded without publication or a tag.
  - P26 is closed by exact-main truth on b638f611363b14ac645e06218b8c8cb4b1906732; P27 is active and P28 remains planned until P27 is protected-merged, exact-main certified, and closed by bcf truth.
  active_artifacts: {spec: plans/product-spec.yml, build_plan: plans/build-plan.yml, active_phase_ledger: plans/phase-ledger.yml, phase_history: plans/phase-history.yml, active_phase_plan: plans/phase-27-plan.yml, active_workitem_ledger: plans/phase-27-workitems.yml, active_phase_log: phases/phase-27-log.yml}
known_constraints:
- candidate code runs only on disposable workers; trusted workers reconstruct provider state and never check out candidate code
- routine controller rotation becomes active only after its dormant provider compiler is installed through the one-time ordinary N/N+1 migration; provider evidence never grants release authority
- legacy receipt v2 and session v1 evidence remains readable only for its exact original subject
- unknown dependency applicability fails closed to a declared conservative workload
- provider single-object limits archive expansion limits free-space requirements timeouts bounded parallelism and exact owned-resource cleanup remain safety controls
- isolated adversarial and integration execution state is session-scoped and exactly retired; unexplained state fails before execution and persistent sharing requires an explicit workload contract
- publication remains a separate write-authorized exact-byte boundary
reusable_commands: [.venv/bin/python scripts/validate_governance_yaml.py --repo-root ., .venv/bin/python -m pytest -q tests, bcf migrate-contract --repo-root . --check]
references:
  governance: [AGENTS.yml, governance-profile.yml, governance/gate-contracts.yml, governance/evidence-policy.yml, governance/ci-graph.yml, plans/phase-ledger.yml, plans/phase-history.yml]
  product: [plans/product-spec.yml, plans/build-plan.yml]
  execution: [plans/phase-26-plan.yml, plans/phase-26-workitems.yml, phases/phase-26-log.yml, plans/phase-27-plan.yml, plans/phase-27-workitems.yml, phases/phase-27-log.yml, plans/phase-28-plan.yml, plans/phase-28-workitems.yml, phases/phase-28-log.yml]
