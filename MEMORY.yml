document:
  kind: durable_project_memory
  name: "BCF Governance Working Memory"
  id: "bcf-governance-memory"
  version: 1.0.0
  generated_at_utc: "2026-08-14T00:00:00Z"
  status: active
  path: MEMORY.yml
regression_shield:
  purpose: keep this file as durable project memory, not as an execution transcript
  invariants:
    - store stable decisions, constraints, environment facts, reusable commands, and references
    - keep workitem execution evidence in phases/*.yml
    - update high-signal entries instead of duplicating them
    - never record secrets or local environment values here
stable_decisions:
  canonical_repo_root: "."
  canonical_governance_guide: AGENTS.yml
  canonical_product_spec: plans/product-spec.yml
  canonical_build_plan: plans/build-plan.yml
  canonical_active_phase_ledger: plans/phase-ledger.yml
  canonical_phase_history: plans/phase-history.yml
  canonical_phase_log_root: phases/
  code_roots: {backend: bcf_governance, frontend: docs}
  delivery_architecture: {backend: "python_package_with_private_portable_runtime", frontend: "canonical_operator_and_maintainer_guides", data: "yaml_json_contracts_and_content_addressed_evidence", observability: governed_reference_contracts}
  governance_process:
    semantic_validator: scripts/validate_governance_yaml.py
    structural_schema_root: schemas/
    hotfix_lane: governed_and_machine_readable
    artifact_ownership: canonical_owner_map_recorded_in_AGENTS
    phase_rollover: phase_ledger_and_MEMORY_must_move_together
    build_artifact_convention: follow_local_template_pack_shapes_and_naming
memory_retention_policy:
  stable_decisions:
    review_frequency: per_phase_rollover
  environment_facts:
    revalidate_after_days: 7
  reusable_commands:
    validate_on_release_gate: true
  stale_entry_handling:
    - mark_deprecated_before_removal
    - preserve_replacement_reference_when_available
environment_facts:
  current_repo_state:
    - BCF 0.6.1 standard self-governance adopted on 2026-08-14
    - all governance, memory, specs, plans, workitem ledgers, and phase logs are YAML-native
    - observability contracts are scaffolded under contracts/observability/v1/
    - structural anti-drift gates are scaffolded for module size, layer ownership, bounded-context ownership, CQRS side rules, router thinness, and duplication
    - v0.7.0 is an annotated unsigned tag at 0e965b2197e776ad411b67820dbd5586263abde1; release 379785364 is published with three attested assets but reports immutable false and is not certified under corrected authority v1.1
    - P13-HF03 merged as de2eeee6c16d074c4c5fffd39ee5d583b36d1a3e; provider-authenticated bootstrap 33513097208 and probe 33513145558 proved it on both trusted runners, promotion PR 86 merged as 6673c6ba7028162e6f0385e1866810559a52e802, and exact-main 33514102987, finalizer 33514433224, publisher 33514473576, scheduled mutants 33514518560 and 33514521143, authority canaries 33514557778 and 33514632374 attempts 1 and 2, release build 33514881671, verifier 33515210677, and collector 33515519688 completed the pre-reconciliation acceptance rehearsal without enabling publication
    - the P13 structural cutover retains authority-v1 readers, requires v1.1 for new exact-main and release commands, and keeps release execution unreachable until provider workflow identities are pinned and activated
    - P01 through P12 roadmap detail is owned only by plans/phase-history.yml; active product and build roadmaps contain P13 and mechanically validate their history boundary at P12
    - P13 preserves 21 gates while exact manifests and cause controls ratchet mechanically; workflow identity, admission roles, privileged jobs, controller custody, and release lock/manifest blobs are compiled from Git/provider bytes, verifier success binds controller-owned offline wheel/sdist/Twine execution and raw evidence, profile-v2 evidence semantics have one gate-contract owner, local PR and scheduled mutant entrypoints select canonical preflight, scheduled runs retain exact-subject artifacts, interpreter/venv identity is checked before evidence or mutants, and named mutants run only exact oracle nodes after one full positive baseline
    - provider-compiled bootstrap 33464640838 plus probe 33464672319 prove 63c7c180 installed on both trusted runners; after PR 55 merged as de8b3ce25f52916e6795a2b9240be969de932788, promoted-controller exact-main 33465739678 passed admission and every producer before active-phase closure refusal, and finalizer 33465936849 plus publisher 33465963703 proved the terminal control path
    - P13 activation PR 44 merged as 35f3ab4ff9fa6b91ac20f706df0cc42def6a1718; exact-main run 33440869640 proved admission but release preflight rejected its incorrectly inherited PR mode before evidence fanout, so exact-main reusable governance must select closure truth and release preflight
    - exact-main admission and package-producer bytes are fixed at 82dc4f740d9fb2b5920cc66037f0885614b351df; governance reusable-mode bytes remain fixed at 356196fe7fdeca1bd0de80c0d58f198e1476f716, each pinned independently by exact Git blob and SHA-256 identity
    - the 0.7.1 release closure pins Ubuntu 24.04, CPython 3.12.14, pytest 9.0.3, and 41 direct/transitive Linux x86-64 wheels by filename and SHA-256
    - every candidate job uses a fresh standard GitHub-hosted VM; the public repository receives free unlimited standard-runner execution and has no self-hosted fallback
    - main is protected by active repository ruleset 21862678 requiring pull requests, current governance-truthfulness, pack-checks, ast-checks, resolved conversations, and no force push or deletion
    - evidence execution uses the explicitly selected Python and propagates its lexical executable directory and required loader environment into detached positive and negative-control worktrees
    - P03's canonical preflight runs before evidence; exact contract-owned pytest manifests and run-attempt-specific, disjoint artifact namespaces prevent stale fan-in while profile-v2 truth independently verifies immutable session bindings
    - strict git-history phase retention is selected; completed triplets are compacted with exact commit and SHA-256 custody and no retroactive derived state
    - hosted Identity proof 33487315694 attempt 1 and artifact 9792372773 passed on exact Identity commit 0569e6dd81b311abf33af5626a32441b0d55cc64 with 62 representations, 221 traces, zero uncovered required traces, zero violations or escapes, and median 8.842728 seconds; audits/p13-identity-hosted-validation.json is generated only from authenticated provider records and proof bytes, and Git comparison proves every packaged semantic-ownership engine and reference-contract byte unchanged through repaired controller de2eeee6
    - corrected live-inventory controller f3c4eacbc9900a8b77d36384e0c19112073c12ac was selected from exact-main artifact 9794142932 by provider resolution and checksum compilation, then independently installed and probed on both trusted runners by runs 33492766642 and 33492854861 before promotion
    - release-input controller 46180ab was provider-compiled from exact-main artifact 9796508831, independently proven by bootstrap 33498812446 and probe 33498854324 on both trusted runners, and promoted by PR 81; exact-main 33499356117 then passed every producer and all 21 receipts before lifecycle-only refusal
    - P13-HF04 makes publication activation mechanically derive the newest exact-main collector attempt, receipt artifact identity, current commit and tree, version-owned tag, and closed asset inventory; no operator-entered provider coordinate can select release authority
  active_artifacts:
    spec: plans/product-spec.yml
    build_plan: plans/build-plan.yml
    active_phase_ledger: plans/phase-ledger.yml
    phase_history: plans/phase-history.yml
    active_phase_plan: plans/phase-13-plan.yml
    active_workitem_ledger: plans/phase-13-workitems.yml
    active_phase_log: phases/phase-13-log.yml
known_constraints:
  - schema-2 evidence must be recaptured after every governed tree mutation
  - template runtime and packaged template copies must remain byte-identical to canonical sources
  - receipt invocation remains canonical while selected-interpreter identity is recorded as hashed environment metadata
  - release tags are created only at a merge commit whose source and governance CI passed; v0.7.1 is annotated unsigned by policy
  - standard hosted runners are free and unlimited while the repository remains public; larger-runner and artifact-storage costs remain separate
  - the persistent trusted VM may collect provider state and publish but may never check out or execute candidate code
  - "P10 exact-main authority is live: P10-HF14 merged as ef8e8ee1d9eda4da77eafdb99a8846ccf2e84cd2, admission run 33359863185 and producer runs 33359863183/33359863142 were reconstructed by finalizer 33360174498, and publisher 33360197707 wrote the successful bcf/exact-main-certification status; controller artifact 9744774824 remains installed on both trusted runners"
  - v0.7.0 tag, release, assets, and attestations are immutable historical custody from BCF's perspective even though GitHub reports that release mutable
  - repository immutable-release settings, v0.7.1 tag creation, and publication require explicit owner approval after corrected exact-main and release authority evidence passes
  - hosted jobs run to completion without polling, sleeping, idle waiters, or route fallback
  - every BCF behavior commit updates and validates BCF's own applicable governance contracts before successor work begins
  - cheap preflight validates every declared negative-control target and derives tracked Python entrypoints before allocating evidence; moved controls and module-time BCF imports without the correct source-root bootstrap fail before detached, package, mutant, or release work, and editable installs are never authority
  - profile_contract_version absence means 1.0; normal upgrades preserve the installed profile and contract version, and v2 promotion is explicit and monotonic
  - profile promotion never rewrites installed workflows; GitHub workflow adoption remains a separate explicit transaction
  - fresh Lite installations default to v1 while fresh Standard and Regulated installations default to v2
  - expired evidence sessions are pruned only from the ignored fixed session root under declared retention with immediate identity revalidation
  - BCF Standard-v2 evidence preserves its four mechanically derived hosted shards while binding every receipt to one preflight session and exact run attempt
  - TypeScript source analysis and external producer acquisition use typed N/A until their tracked-path triggers make them applicable
  - normalized CI certification is an input to truth and cannot claim certified; only independently verified truth may emit the final output-only release receipt
  - PR truth evaluation may report an exact-tree change eligible while its phase train remains active, but it preserves the lifecycle, cannot report release closure, and cannot emit a release receipt; closure evaluation remains the default and sole release authority
  - cleanup revalidates exact Docker IDs/run labels before scoped deletion; retention-removable phase artifacts are consumed only through phase-history plus exact Git commit/hash custody, never live fixture paths
reusable_commands:
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  - .venv/bin/python -m pytest tests
  - bcf profile promote --repo-root . --to standard --contract-version 2.0 --check
  - bcf ci-cleanup --repo-root . --prune-evidence-sessions
references:
  governance: [AGENTS.yml, governance-profile.yml, architecture-boundaries.yml, schemas/, contracts/observability/v1/, plans/phase-ledger.yml, plans/phase-history.yml, docs/USAGE.md, docs/MAINTAINING.md, docs/ARCHITECTURE.md, docs/CI_AUTHORITY.md]
  product: [plans/product-spec.yml, plans/build-plan.yml]
  execution: [plans/phase-13-plan.yml, plans/phase-13-workitems.yml, phases/phase-13-log.yml, phases/phase-13-hotfix01.yml, phases/phase-13-hotfix02.yml, phases/phase-13-hotfix03.yml]
