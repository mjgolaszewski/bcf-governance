document:
  kind: execution_phase_plan
  name: Phase 11 Plan
  id: bcf-governance-phase-11-plan
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: plans/phase-11-plan.yml
phase:
  id: P11
  build_block: exact-main-release-artifacts
  planner: codex
  date: '2026-08-30'
  scope_source:
  - AGENTS.yml
  - plans/product-spec.yml
  - plans/build-plan.yml
  - plans/phase-ledger.yml
  - MEMORY.yml
delivery_contract:
  tightly_scoped_deliverables:
  - owner-and-main-only trusted authorization from the latest exact-main certification
  - clean 0.7.0 wheel and sdist proof on one fresh hosted candidate VM
  - exact artifact hashes and CI certification bound to an output-only release receipt
  - durable certified artifact upload without tag creation or publication
  parallelizable_workstreams:
  - id: P11-WS1
    name: artifact build and certification
  hard_dependencies:
  - P10
  docker_first_local_runtime: repo_native_validation_and_governance_commands
  completeness_standard: full_completeness_for_declared_scope
scope_lock:
- order: 1
  statement: keep the phase narrowly scoped to release authorization, artifact construction,
    artifact verification, and receipt binding
- order: 2
  statement: keep tag creation, artifact publication, attestation, and the README-led editorial
    refresh disabled until P12
- order: 3
  statement: trusted release authorization checks out and executes no repository code; candidate
    build execution remains disposable, read-only, and non-publishing
verification_plan:
- .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
- .venv/bin/python -m pytest tests
- .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
- .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
