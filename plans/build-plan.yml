document: {kind: machine_readable_build_plan, name: BCF Governance Build Plan, id: bcf-governance-build-plan, version: 1.0.0, generated_at_utc: '2026-08-14T00:00:00Z', status: active, path: plans/build-plan.yml}
planning_contract:
  format: yaml
  mandatory_properties: [tightly_scoped_deliverables, parallelizable_workstreams, hard_dependency_ordering, docker_first_local_runtime, ast_architecture_tests, strict_boundaries, observability_reference_model, completeness_standard]
  completeness_standard: full_completeness_for_declared_scope
planning_inputs:
  date: '2026-09-14'
  planner: codex
  primary_source_of_truth: plans/product-spec.yml
  governance_references: [AGENTS.yml, MEMORY.yml, plans/phase-ledger.yml]
global_delivery_rules: [change units default to the smallest valid vertical slice with low blast radius, 'every behavior change must update specs, plans, MEMORY, and tests together', preserve public contracts unless an explicit instruction and migration note authorize a breaking change, release gates must run in a declared runtime environment, governed artifacts must pass structural schema validation before semantic cross-artifact validation, 'architecture boundaries must be encoded in tests, not left as convention-only prose', packaged implementation stays under bcf_governance and public source scripts remain thin wrappers, 'canonical tooling, standalone private runtime, template source, and packaged template copies stay synchronized', 'governance changes must pass semantic validation, not only YAML parse checks', observability changes must preserve the shared telemetry and logging contract baseline, 'active phase lifecycle uses the governed taxonomy and records control metadata when work is blocked, paused, or abandoned', governed build artifacts must follow the template-pack naming and validation conventions, completed release trains must preserve governed phase history for each declared phase, new abstractions require proven reuse across at least two real call sites unless explicitly authorized, emergency red-CI and release-blocking fixes must use the governed hotfix lane, lite hotfix mode is reserved for a single-commit repair with no public-contract or security-scope change; all other hotfixes use full mode, candidate code never executes on persistent or trusted control-plane runners, profile-v1 public contracts remain compatible until a repository explicitly promotes to contract v2, 'provider state, evidence sessions, certification, and release receipts form an acyclic verification chain', 'trusted CI workflow, run, attempt, commit, tree, session, and bundle identity are reconstructed and compared mechanically before any status or release mutation', 'each BCF behavior commit updates and validates BCF''s own applicable governance contracts before successor work begins; local implementation may proceed while hosted Actions are unavailable, but remote candidate execution and publication stay disabled until disposable and trusted substrate acceptance is proven']
global_gates:
- id: G1
  name: governed_foundation
  applies_before: P02
  required_completion_criteria: [canonical governance artifacts exist, phase ledger points to active phase artifacts, semantic governance validation passes, architecture boundary test lane exists]
- id: G2
  name: safe_execution_substrate
  applies_before: P10
  required_completion_criteria: [trusted control-plane VM executes no candidate code, every code-executing job receives a fresh disposable VM and ephemeral runner identity, no GitHub-hosted fallback is configured]
phase_history: {path: plans/phase-history.yml, through_phase: P26}
phase_sequence:
- phase_id: P27
  build_block: minimum-time-proof-composition-2.1.0
  objective: minimize certification critical-path time using P26 proof transport while preserving assurance semantics
  hard_dependencies: [P26]
  tightly_scoped_deliverables: [selective execution planner with qualification-preserving negative-control reuse and a canonical assurance-owner inventory, measured constrained critical-path DAG with duration-aware assignment and one-PR routine controller rotation, canonical convergent bcf reconcile fixed-point generation with controlled demolition of redundant ceremony, schema-validated timing telemetry routine-rotation proof empirical benchmarks and certified closeout]
  parallelizable_workstreams: [selective execution planning and assurance inventory, critical-path composition assignment and controller-transition migration, fixed-point generation and controlled demolition, telemetry rotation benchmarking and closeout]
  verification_commands: [.venv/bin/python scripts/validate_governance_yaml.py --repo-root ., .venv/bin/python -m pytest -q tests]
- phase_id: P28
  build_block: canonical-projection-parity-certification-2.1.0
  objective: project BCF 2.1 canonically prove self-adopter parity measure performance and publish immutable 2.1.0
  hard_dependencies: [P27]
  tightly_scoped_deliverables: [normal install and adopt projection of P26 and P27 semantics, typed narrow self-authority overlays with enforced non-leakage, representative adopter parity and adversarial proof, empirical SLO immutable 2.1.0 publication custody and certified closeout]
  parallelizable_workstreams: [canonical installation and adoption projection, typed self-authority overlays, self-adopter parity and attacks, empirical certification release and custody closeout]
  verification_commands: [.venv/bin/python scripts/validate_governance_yaml.py --repo-root ., .venv/bin/python -m pytest -q tests]
