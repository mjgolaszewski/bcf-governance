document:
  kind: execution_phase_plan
  name: Phase 08 Plan
  id: bcf-governance-phase-08-plan
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: planned
  path: plans/phase-08-plan.yml
phase:
  id: P08
  build_block: github-adopter-runtime-pr-parity
  planner: codex
  date: '2026-08-30'
  scope_source:
  - AGENTS.yml
  - plans/product-spec.yml
  - plans/build-plan.yml
  - plans/phase-ledger.yml
  - MEMORY.yml
delivery_contract:
  tightly_scoped_deliverables:
  - GitHub reference pack, transactional adopter, runtime contracts, and local PR
    helper
  parallelizable_workstreams:
  - id: P08-WS1
    name: GitHub adapter and runtime parity
  hard_dependencies:
  - P07
  docker_first_local_runtime: repo_native_validation_and_governance_commands
  completeness_standard: full_completeness_for_declared_scope
scope_lock:
- order: 1
  statement: keep the phase narrowly scoped to declared deliverables and update canonical
    governed artifacts in the same change
- order: 2
  statement: preserve all existing workflows byte-for-byte unless the explicit transactional GitHub adopter owns their closed managed paths
- order: 3
  statement: ship the reference topology and operator contracts without activating BCF self-adoption before P10
- order: 4
  statement: candidate jobs use disposable read-only workers while trusted roles never check out or execute candidate code and never poll sleep or idle
verification_plan:
- .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
- .venv/bin/python -m pytest tests
