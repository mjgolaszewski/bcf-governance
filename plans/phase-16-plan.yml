document:
  kind: execution_phase_plan
  name: Phase 16 Plan
  id: bcf-governance-phase-16-plan
  version: 1.0.0
  generated_at_utc: '2026-09-02T00:00:00Z'
  status: planned
  path: plans/phase-16-plan.yml
phase:
  id: P16
  build_block: stable-packaging-and-support-1.0.0
  planner: codex
  date: '2026-09-02'
  scope_source:
  - AGENTS.yml
  - plans/product-spec.yml
  - plans/build-plan.yml
  - plans/phase-ledger.yml
  - MEMORY.yml
delivery_contract:
  tightly_scoped_deliverables:
  - record immutable 1.0.0rc1 provider custody and compact P15 under exact Git hashes
  - promote the frozen package and release contract from 1.0.0rc1 to 1.0.0
  - build verify attest and publish exact certified stable bytes without rebuild
  parallelizable_workstreams:
  - id: P16-WS1
    name: release-candidate custody and phase retention
  - id: P16-WS2
    name: stable version and support declaration parity
  - id: P16-WS3
    name: exact-main stable artifact certification and immutable publication
  hard_dependencies:
  - P15
  docker_first_local_runtime: repo_native_validation_and_governance_commands
  completeness_standard: full_completeness_for_declared_scope
scope_lock:
- order: 1
  statement: keep the phase narrowly scoped to declared deliverables and update canonical
    governed artifacts in the same change
verification_plan:
- .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
  --python .venv/bin/python --format text
- .venv/bin/python -m pytest -q tests
- .venv/bin/python .github/scripts/run_validator_mutants.py --profile full
- .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-full
