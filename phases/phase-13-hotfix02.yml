document:
  kind: hotfix_execution_log
  name: P13-HF02 Hotfix Log
  id: project-phase-13-hotfix02
  version: 1.0.0
  generated_at_utc: '2026-09-01T00:00:00Z'
  status: planned
  path: phases/phase-13-hotfix02.yml
hotfix:
  id: P13-HF02
  mode: full
  related_phase_id: P13
  hotfix_number: 2
  summary: Make source-distribution workflow custody portable and preserve verifier
    failure evidence
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
    --python .venv/bin/python --format text
  executed_commands:
  - command: publication-disabled release 33500913722; verifier 33501188756
    result: expected_fail_remediated
    notes: authenticated exact-main authorization and build passed; the secretless verifier failed only two source-distribution tests that duplicated unavailable definition-commit history, and no collector authority was emitted
  - command: canonical release preflight before and after hash-closed environment repair
    result: pass
    notes: the first committed run failed in 1.7 seconds on missing setuptools and created no evidence session; after installation from the verified 41-wheel closure, preflight reported exact setuptools 84.0.0 and wheel 0.48.0 before all later work
  - command: governed source and contract-test manifests; exact contract evidence
    result: pass
    notes: exact head 7a9b629 passed all 537 source nodes; its contract population contains 272 exact nodes, and the preceding exact receipt passed 102 of 102 cause-verified controls before the final virtualenv fixture ratchet
  - command: governed wheel and source-distribution artifact harness
    result: pass
    notes: exact 7a9b629 wheel install, installed-pack governance truth, standalone truth, and extracted-sdist suite passed; the sdist reported 535 passes and exactly its two declared Git-custody skips
  - command: bcf ci local-pr --repo-root .
    result: pass
    notes: default branch, ancestry, PR base SHA, event context, changelog rule, and selected-interpreter preflight were mechanically derived for 7a9b629
  - command: pull-request runs 33505006870 and 33505006839
    result: expected_fail_remediated
    notes: both jobs rejected the selected environment in 11 to 12 seconds before evidence or package work because the independently maintained bootstrap list omitted governed build requirements; the truth job then exposed a separate missing-fan-in reporting defect
  notes:
  - publication-disabled release run 33500913722 built exact 652df02 bytes, then
    verifier 33501188756 failed only sdist-tests after 529 passes because two tests
    independently required definition commits unavailable from an extracted archive
  - P13-HF02 keeps normal source validation fail-closed on exact Git ancestry, gives
    extracted sdists a mechanically recognized one-commit package context, delegates
    workflow byte and inventory checks to the canonical verifier, and uploads failed
    runtime evidence with three cause-verified controls
  - the local artifact harness then proved cheap preflight omitted build-system
    requirements and failed at setuptools.build_meta; preflight now derives those
    requirements from pyproject.toml before allocating any session or evidence work
  - the next sdist run exposed an environment-dependent symlinked-venv fixture; that
    test now isolates the symlink invariant, while a separate cause-verified test keeps
    ambient VIRTUAL_ENV mismatch rejection authoritative
  - the interpreter plan now single-owns the sorted project, optional, build-system,
    and gate dependency union; requirements-governance.txt is its generated bootstrap
    projection, and both setup and preflight consume or independently verify that owner
  - failed preflight or evidence prerequisites now skip truth fan-in and emit an exact
    run-attempt terminal observation, preserving the real cause without missing-artifact
    noise or allocating evidence work
  - exact local session fd6eb703381118ff1b9932a584410876 produced all 21 passing
    gate receipts and 137 cause-verified controls; invoking truth on the retained parent
    root correctly rejected mixed sessions, exposing that generated release-check still
    selected the parent, so the generator and BCF's exact generated surface now select
    only the newly allocated session
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
