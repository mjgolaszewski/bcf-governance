document:
  kind: hotfix_execution_log
  name: P10-HF06 Hotfix Log
  id: bcf-governance-phase-10-hotfix06
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-10-hotfix06.yml
hotfix:
  id: P10-HF06
  mode: lite
  related_phase_id: P10
  hotfix_number: 6
  summary: Give mechanically derived evidence shards descriptive operator-facing names
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_self_governance_contracts.py::test_governance_evidence_shards_derive_every_required_gate_once
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate contract-test --session-manifest <fresh-local-session>
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  executed_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate contract-test --session-manifest <fresh-local-session>
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  notes:
  - lite mode is appropriate because this is a single-commit operator-display repair with no public contract or security-boundary change
  - preserve the stable governance-truthfulness, pack-checks, and ast-checks names required by the main ruleset
  - keep numeric shard coordinates and artifact namespaces stable while exposing the canonical display name in the check suite
  - make the capture script the semantic owner of display names and mechanically reject a drifting workflow mirror
  - the first focused receipt correctly rejected the new control because its killer node was absent from the positive contract baseline; the governed selector and exact manifest were repaired before closeout
  - 377 full tests pass; a fresh committed-tree receipt passes 107 exact contract nodes and all 18 controls
  - the display-name mutant applies exactly, exits 1, fails only the declared shard-name parity node, and leaves no unexpected changes
  - high-value implementation and semantic mutants pass together with strict validation and exposure scanning
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
