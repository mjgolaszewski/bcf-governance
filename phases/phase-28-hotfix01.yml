document:
  kind: hotfix_execution_log
  name: P28-HF01 Hotfix Log
  id: project-phase-28-hotfix01
  version: 1.0.0
  generated_at_utc: '2026-09-26T00:00:00Z'
  status: completed
  path: phases/phase-28-hotfix01.yml
hotfix:
  id: P28-HF01
  mode: full
  related_phase_id: P28
  hotfix_number: 1
  summary: Stabilize exact provider artifact inventory snapshots during concurrent
    trusted uploads
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_ci_github_controller.py
    tests/test_routine_controller_provider.py
  executed_commands:
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  - .venv/bin/python -m pytest -q tests/test_profile_contract_v2.py tests/test_self_governance_contracts.py tests/test_ci_github_controller.py tests/test_routine_controller_provider.py
  - PYTHONPATH=. .venv/bin/python -m bcf_governance.cli reconcile --repo-root . --python .venv/bin/python --check
  - PYTHONPATH=. .venv/bin/python -m bcf_governance.cli ci prospective-train --repo-root . --python .venv/bin/python --intent workitem --target P28-P0-01 --subject-commit $(git rev-parse HEAD) --subject-tree $(git rev-parse HEAD^{tree}) --format json
  notes:
  - Exact-main admission 36217997666/1 built the provider-authenticated N+1 controller
    for main 98716225814b305658060e5bc5b2589222ff1f17 and correctly suppressed
    semantic evidence under ordinary pending rotation.
  - Routine authorizer 36218038519/1 failed before bootstrap because trusted
    finalizer 36218038537/1 uploaded its certification bundle while the authorizer
    paginated the repository-wide Actions artifact inventory.
  - The existing exact inventory guard correctly rejected mixed pagination but had
    no bounded path to establish a later stable provider snapshot.
  - The canonical artifact inventory primitive now requires two consecutive exact
    inventories, restarts after bounded within-read drift, and rejects persistent
    churn, malformed identities, incomplete pages, duplicates, and same-count
    replacement.
  - Controller policy, provider authority, graph topology, workflows, release
    authority, protection, and certification semantics remain unchanged.
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - governance-validate
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
