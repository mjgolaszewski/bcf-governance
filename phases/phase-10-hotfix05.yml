document:
  kind: hotfix_execution_log
  name: P10-HF05 Hotfix Log
  id: bcf-governance-phase-10-hotfix05
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: planned
  path: phases/phase-10-hotfix05.yml
hotfix:
  id: P10-HF05
  mode: full
  related_phase_id: P10
  hotfix_number: 5
  summary: Restore selected-interpreter loader authority for trusted controller probes
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_self_governance_contracts.py::test_trusted_jobs_never_checkout_or_invoke_candidate_scripts
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate contract-test --session-manifest <fresh-local-session>
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - owner-dispatch bcf/trusted-control-probe from exact main
  executed_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate test --session-manifest <superseded-broad-local-session>
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  notes:
  - bootstrap run 33341681969 installed and executed the exact controller successfully on both trusted runners
  - probe run 33341705849 failed on both runners because its job omitted the setup-python loader environment required by the persisted tool-cache interpreter
  - activation remains disabled; no callback, status, cancellation, or publication authority was enabled
  - the runner policy is the canonical owner of the exact setup-python action, Python version, and every workflow that invokes the persisted controller
  - 377 exact tests pass; the first committed-tree test-gate receipt killed the interpreter mutant but replayed the broad test population and was superseded by focused contract-gate ownership before merge
  - focused contract-gate recapture is required before this hotfix returns to completed
  - a fresh exact-main probe remains post-merge acceptance evidence
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
