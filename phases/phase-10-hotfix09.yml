document:
  kind: hotfix_execution_log
  name: P10-HF09 Hotfix Log
  id: bcf-governance-phase-10-hotfix09
  version: 1.0.0
  generated_at_utc: '2026-08-31T00:00:00Z'
  status: completed
  path: phases/phase-10-hotfix09.yml
hotfix:
  id: P10-HF09
  mode: full
  related_phase_id: P10
  hotfix_number: 9
  summary: Add acyclic trusted callback envelopes
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_ci_github_controller.py tests/test_cli.py tests/test_pack_sync.py
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate contract-test --session-manifest <fresh-local-session>
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  executed_commands:
  - .venv/bin/python -m pytest -q tests/test_ci_github_controller.py tests/test_cli.py tests/test_pack_sync.py
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_self_governance_gate.py contract-test
  - .venv/bin/python .github/scripts/run_self_governance_gate.py architecture-test
  - .venv/bin/python .github/scripts/run_self_governance_gate.py semantic-ownership
  - .venv/bin/python .github/scripts/run_self_governance_gate.py lint
  - .venv/bin/python .github/scripts/run_self_governance_gate.py typecheck
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  notes:
  - full mode is required because this slice adds trusted callback behavior and additive public CLI operations
  - every finalizer callback produces one exclusively created immutable envelope; pending envelopes carry no candidate artifacts and terminal envelopes bind the closed bundle-manifest digest
  - the publisher authenticates the exact triggering finalizer run and attempt; pending completion is a deterministic no-op and only a verified terminal envelope reaches existing status authority
  - callback status, bundle digest, collector identity, and top-level inventory each fail closed before publication
  - the callback envelope intentionally does not duplicate admission state or producer inventory; those remain mechanically owned and recomputed by the certification bundle
  - the canonical GitHub run identity registry names the callback authorization consumer and all generated source mirrors
  - profile promotion renders the expanded evidence policy at a deterministic 132-column width so all controls remain within the existing hard context budget
  - all 392 exact test-manifest nodes and 128 exact contract nodes pass; four callback trust-boundary controls are declared
  - existing finalize and publish operations remain supported; active workflows, repository status publication, release publication, and candidate execution are unchanged in this structural slice
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
