document:
  kind: hotfix_execution_log
  name: P22-HF01 Hotfix Log
  id: bcf-governance-phase-22-hotfix01
  version: 1.0.0
  generated_at_utc: '2026-09-11T00:00:00Z'
  status: planned
  path: phases/phase-22-hotfix01.yml
hotfix:
  id: P22-HF01
  mode: full
  related_phase_id: P22
  hotfix_number: 1
  summary: Correct source-root qualification of imported Python semantic constructors
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  executed_commands: []
  notes:
  - issue 159 is a high-severity upstream fail-open defect reproduced against exact BCF 1.1.0 source and release bytes
  - repository-relative module identity diverges from absolute import identity below src-style roots and exact symbol comparison silently omits the imported constructor
  - remediation owns explicit import-root qualification after independent tracked-source discovery and rejects unsafe ambiguous or conflicting roots
  - Racecar remains read-only and receipt profile CI graph authority and gate identifiers are excluded
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
