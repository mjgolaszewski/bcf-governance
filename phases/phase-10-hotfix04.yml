document:
  kind: hotfix_execution_log
  name: P10-HF04 Hotfix Log
  id: bcf-governance-phase-10-hotfix04
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-10-hotfix04.yml
hotfix:
  id: P10-HF04
  mode: full
  related_phase_id: P10
  hotfix_number: 4
  summary: Repair non-relocatable trusted controller virtualenv bootstrap
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_self_governance_contracts.py
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - owner-dispatch bcf/trusted-control-bootstrap and bcf/trusted-control-probe from exact main
  executed_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_self_governance_gate.py contract-test
  - .venv/bin/python scripts/governance_evidence.py --repo-root . run --gate contract-test --session-manifest <fresh-local-session>
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  - bash syntax validation for the trusted bootstrap install block
  - fresh, idempotent, and broken-shebang recovery execution against artifact 9740097978 in disposable local tool-cache roots
  notes:
  - bootstrap run 33340656638 authenticated and downloaded the exact artifact on both trusted runners before both offline installs failed with exit 127
  - the installed console script retained the deleted staging virtualenv in its absolute shebang after the workflow renamed the environment
  - authority callbacks, statuses, cancellation, and publication remained disabled throughout remediation and stay disabled until post-merge bootstrap and probe acceptance
  - the repair creates the virtualenv only at its final commit-addressed path, marks partial ownership by exact run and attempt, and removes only a provenance-matching unusable prior installation or its own incomplete install
  - 377 exact tests and 105 contract nodes pass; all 16 contract controls pass, including the cause-verified relocation mutant; both high-value mutant profiles, structural validation, exposure scan, and shell syntax pass
  - local execution proved fresh install, idempotent reuse, recovery from an exact-provenance broken shebang, and final entrypoint execution; exact-main bootstrap and dual-runner probe remain post-merge acceptance evidence
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
