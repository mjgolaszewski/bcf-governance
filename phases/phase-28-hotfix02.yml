document:
  kind: hotfix_execution_log
  name: P28-HF02 Hotfix Log
  id: project-phase-28-hotfix02
  version: 1.0.0
  generated_at_utc: '2026-09-26T00:00:00Z'
  status: completed
  path: phases/phase-28-hotfix02.yml
hotfix:
  id: P28-HF02
  mode: full
  related_phase_id: P28
  hotfix_number: 2
  summary: Resolve authenticated reuse attestations consistently in hotfix truth closure
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests/test_governance_truth.py
    -k hotfix
  executed_commands:
  - .venv/bin/python -m pytest -q tests/test_governance_truth.py
    -k 'hotfix or reuse' tests/test_truth_reporting.py
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  - PYTHONPATH=. .venv/bin/python -m bcf_governance.cli reconcile
    --repo-root . --python .venv/bin/python --check
  - PYTHONPATH=. .venv/bin/python -m bcf_governance.cli ci
    prospective-train --repo-root . --python .venv/bin/python
    --intent workitem --target P28-P0-01 --subject-commit $(git rev-parse HEAD)
    --subject-tree $(git rev-parse HEAD^{tree}) --format json
  notes:
  - Exact-main 36223127734/2 authenticated reusable producer evidence and computed
    P28-P0-01 closed with P28-P0-02 eligible, but active hotfix truth alone omitted
    the canonical claim-indexed reuse projection.
  - P28-HF01 therefore remained completed because reused test, contract-test, and
    runtime-smoke claims were invisible only to its closeout consumer.
  - The shared hotfix resolver now consumes the same authenticated reuse index as
    phase and bounded-workitem truth; wrong claim or execution-group identity still
    fails closed.
  - No evidence, controller, certification, publisher, protection, or release
    authority semantics are weakened.
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - governance-validate
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
