document:
  kind: hotfix_execution_log
  name: P13-HF06 Hotfix Log
  id: bcf-governance-phase-13-hotfix06
  version: 1.0.0
  generated_at_utc: '2026-09-01T00:00:00Z'
  status: completed
  path: phases/phase-13-hotfix06.yml
hotfix:
  id: P13-HF06
  mode: full
  related_phase_id: P13
  hotfix_number: 6
  summary: Consolidate final 0.7.1 activation under mechanical workflow, environment,
    and publication authority
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
    --python .venv/bin/python --format text
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  executed_commands:
  - command: trusted bootstrap run 33524862289 attempt 1
    result: pass
    notes: provider-authenticated bootstrap installed controller 49272182790342a52ef7ddbcfd6a540ca7df6baf independently on both uniquely labeled trusted runners
  - command: trusted probe run 33524915966 attempt 1
    result: pass
    notes: provider-authenticated probe confirmed the exact controller installation on both trusted runners
  - command: bcf ci sync-self-controller --pin compiled-pin.json --confirmation compiled-confirmation.json --apply
    result: pass
    notes: the projector promoted only provider-compiled controller custody after exact two-runner proof; no run, attempt, artifact, commit, tree, or digest was transcribed
  - command: .venv/bin/python -m pytest -q tests/test_self_workflow_contracts.py tests/test_release_workflow_contracts.py tests/test_preflight.py tests/test_ci_self_controller.py tests/test_pack_sync.py
    result: pass
    notes: 50 focused mechanical-workflow, publication, preflight, controller, and generated-copy tests passed
  - command: .venv/bin/python -m pytest -q tests
    result: pass
    notes: all 557 mechanically discovered source-test nodes passed after the generated pack was rebuilt once from its canonical sources
  - command: bcf ci pin-authority --definition-commit 80a32fc557aae9a9f8ba9ee17a3e7e52bf0edf01 --apply
    result: pass
    notes: the compiler derived all 12 privileged workflow blob OIDs, SHA-256 digests, exact job inventories, and definition custody from frozen commit A
  notes:
  - one production preflight owner now decodes BCF workflow inventory, trust routes,
    activation, interpreter, action pins, controller identity, and publisher wiring
  - obsolete tests that independently decoded those same YAML fields were removed;
    distinct public-contract, integration, and causal mutation coverage remains
  - final exact inventories are 21 gates, 557 source nodes, 286 contract nodes, and
    147 cause-verified negative controls
  - the trusted publisher consumes only resolver-owned outputs and the exact closed
    collector asset directory; it performs no checkout, package execution, or rebuild
  - audits/p13-remediation-execution-audit.yml owns the aggregate delivery incident
    metrics and corrective design, keeping release notes and durable memory concise
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
