document:
  kind: hotfix_execution_log
  name: P14-HF02 Hotfix Log
  id: bcf-governance-phase-14-hotfix02
  version: 1.0.0
  generated_at_utc: '2026-09-02T00:00:00Z'
  status: completed
  path: phases/phase-14-hotfix02.yml
hotfix:
  id: P14-HF02
  mode: full
  related_phase_id: P14
  hotfix_number: 2
  summary: Close release CLI operation namespace ownership before release execution
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests
  executed_commands:
  - command: release authority run 33582461911 attempt 1
    result: build_failed_after_source_tests
    notes: authorization passed on the trusted runner and all 588 source tests passed in the closed CPython 3.12.14 environment; the final release-build CLI dispatch then accessed bundle_output, which belongs only to verify-evidence and collect
  - command: audit every release CLI operation parser and dispatch branch
    result: pass
    notes: all ten operations now execute through a parameterized mechanical namespace contract, including verifier bundle staging on its sole owning operation
  - command: focused release authority, artifact, runtime, and workflow tests
    result: 62_passed
    notes: release build, runtime, verification, collection, inspection, and publication command paths passed without provider or candidate trust-boundary changes
  notes:
  - release CLI dispatch branches own only arguments declared by their operation parser
  - the complete release command family is exercised before another release runner is allocated
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
