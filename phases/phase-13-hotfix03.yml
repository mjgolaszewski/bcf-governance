document:
  kind: hotfix_execution_log
  name: P13-HF03 Hotfix Log
  id: bcf-governance-phase-13-hotfix03
  version: 1.0.0
  generated_at_utc: '2026-09-01T00:00:00Z'
  status: completed
  path: phases/phase-13-hotfix03.yml
hotfix:
  id: P13-HF03
  mode: full
  related_phase_id: P13
  hotfix_number: 3
  summary: Make Python entrypoint import authority fail in cheap preflight
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
    --python .venv/bin/python --format text
  - .venv/bin/python -m pytest tests/test_preflight.py tests/test_release_artifacts.py
    tests/test_self_governance_contracts.py
  executed_commands:
  - command: exact-main admission run 33508892804 attempt 1
    result: expected_fail_remediated
    notes: all 21 governance receipts and exact-tree truth passed; package tests and both mutant profiles passed; the controller wheel built, then its source verifier failed with ModuleNotFoundError before artifact upload
  - command: .venv/bin/python -m pytest -q tests/test_preflight.py tests/test_release_artifacts.py tests/test_self_governance_contracts.py::test_exact_main_controller_wheel_is_built_once_after_pack_checks tests/test_pack_sync.py
    result: pass
    notes: 34 focused tests passed after the entrypoint owner, clean-source invocation, workflow contract, and generated-runtime parity changes
  - command: .venv/bin/python -m pytest -q tests
    result: pass
    notes: all 551 exact source nodes passed after workflow authority was mechanically compiled from commit 2325555cea8aeb798b978ff6585ec45fc579a122
  - command: implementation and semantic high-value mutant profiles
    result: pass
    notes: every maintained validator mutant failed at its declared exact node
  - command: exact contract-test evidence session f5b7e2b5a7175da6994d6256cca4e21b
    result: pass
    notes: all 285 contract nodes and 106 cause-verified controls passed on bd6780232063ab962f9c761cc5acd071ffe91385 with zero skips or failures
  - command: isolated exact-main controller construction reproducer
    result: pass
    notes: the wheel built and python -I source verification accepted its mechanically discovered runtime payload without relying on an editable installation
  - command: PR 84 protected governance and package checks
    result: pass
    notes: the hotfix merged as de2eeee6c16d074c4c5fffd39ee5d583b36d1a3e only after all three protected check names passed
  - command: exact-main 33511582433, finalizer 33511969934, and publisher 33512006400
    result: pass
    notes: the repaired merge passed all 21 required receipts, exact-tree truth, package verification, and the terminal authority path; provider resolution selected controller artifact 9801960819
  - command: rotation PR 85 and exact-main 33512606123
    result: pass
    notes: the provider-compiled target merged as e6eebf7ff17bd9c72cc2b6fd97cf27aa88a0fb0d and again passed package verification, both mutant profiles, all evidence shards, and exact-tree truth
  - command: provider-authenticated bootstrap 33513097208 and independent probe 33513145558
    result: pass
    notes: both uniquely labeled trusted runners installed and independently verified controller de2eeee6c16d074c4c5fffd39ee5d583b36d1a3e; the controller compiled the sole promotion confirmation
  - command: promotion PR 86; exact-main 33514102987; finalizer 33514433224; publisher 33514473576
    result: pass
    notes: controller de2eeee6 became active through mechanically compiled workflow projections; the exact merge passed all 21 receipts, package verification, both PR mutant profiles, exact-tree truth, and the authenticated terminal path
  - command: scheduled mutants 33514518560 and 33514521143; authority canaries 33514557778 and 33514632374 attempts 1 and 2
    result: pass
    notes: high-value and full scheduled profiles passed; the first canary succeeded, the newer same-SHA run and its rerun failed only deterministic producer B while every trusted observer succeeded and enforced revocation plus attempt precedence
  - command: publication-disabled release 33514881671; verifier 33515210677; collector 33515519688
    result: pass
    notes: exact source and closed dependencies produced candidate bytes, a separate token-free runtime verified wheel and sdist, and the no-checkout collector emitted the sole schema-2 release receipt with computed certified state; publication remained unreachable
  notes:
  - the controller build defect was not a package build or hosted environment failure; a file-path Python invocation lacked source-root authority and local editable installation hid it
  - cheap preflight now derives executable Python entrypoints from tracked source and rejects module-time BCF imports that precede the directory-specific repository-root bootstrap
  - the release verifier and a second latent semantic-ownership wrapper now establish source authority explicitly; the exact-main workflow invokes the verifier with isolated Python mode
  - active trusted authority is promoted only from the authenticated provider pin plus the provider-compiled two-runner installation confirmation; no probabilistic operator supplies custody values
  - the existing hosted Identity proof remains exact for the generalized engine because Git comparison proves every packaged semantic-ownership engine and reference-contract byte unchanged since its controller; the repaired source wrapper is not part of that proof path
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
