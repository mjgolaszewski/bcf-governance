document:
  kind: hotfix_execution_log
  name: P13-HF03 Hotfix Log
  id: bcf-governance-phase-13-hotfix03
  version: 1.0.0
  generated_at_utc: '2026-09-01T00:00:00Z'
  status: planned
  path: phases/phase-13-hotfix03.yml
hotfix:
  id: P13-HF03
  mode: full
  related_phase_id: P13
  hotfix_number: 3
  summary: Make Python entrypoint import authority fail in cheap preflight
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
    --python .venv/bin/python --format text
  - .venv/bin/python -m pytest tests/test_preflight.py tests/test_release_artifacts.py
    tests/test_self_governance_contracts.py
  executed_commands:
  - command: exact-main admission run 33508892804 attempt 1
    result: expected_fail_remediated
    notes: all 21 governance receipts and exact-tree truth passed; package tests and both mutant profiles passed; the controller wheel built, then its source verifier failed with ModuleNotFoundError before artifact upload
  - command: .venv/bin/python -m pytest -q tests/test_preflight.py tests/test_release_artifacts.py tests/test_self_governance_contracts.py::test_exact_main_controller_wheel_is_built_once_after_pack_checks tests/test_pack_sync.py
    result: pass
    notes: 34 focused tests passed after the entrypoint owner, clean-source invocation, workflow contract, and generated-runtime parity changes
  notes:
  - the controller build defect was not a package build or hosted environment failure; a file-path Python invocation lacked source-root authority and local editable installation hid it
  - cheap preflight now derives executable Python entrypoints from tracked source and rejects module-time BCF imports that precede the directory-specific repository-root bootstrap
  - the release verifier and a second latent semantic-ownership wrapper now establish source authority explicitly; the exact-main workflow invokes the verifier with isolated Python mode
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
