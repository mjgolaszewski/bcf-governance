document:
  kind: hotfix_execution_log
  name: P15-HF01 Hotfix Log
  id: bcf-governance-phase-15-hotfix01
  version: 1.0.0
  generated_at_utc: '2026-09-02T00:00:00Z'
  status: completed
  path: phases/phase-15-hotfix01.yml
hotfix:
  id: P15-HF01
  mode: full
  related_phase_id: P15
  hotfix_number: 1
  summary: Remove stale installed-controller version authority from release publication
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release --python .venv/bin/python --format text
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile full
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-full
  executed_commands:
  - command: trusted publisher runs 33604513355 and 33604661727
    result: failed_closed_without_provider_mutation
    notes: the first run detected the missing temporary admin credential and the second exposed that installed controller 0.8.0 incorrectly supplied v0.8.0 for certified 1.0.0rc1 archives; both ran only on a trusted local runner and no GitHub release was created
  - command: audit release identity environment provider prerequisites generated runtimes and authority pins as one class
    result: pass
    notes: graph-owned current-main version, archive identity, checksums, receipt custody, tag identity, immutable setting, attestations, and required secret are now ordered mechanical prerequisites; template and private runtimes are exact copies
  notes:
  - no remote retry is permitted until full local gates, all causal controls, generated parity, workflow authority, package verification, and both mutant profiles pass
  - the generated workflow receives its tag from the digest-locked release contract so an older installed trusted controller cannot substitute its own package version
  - the controller independently rejects mixed distribution or version archive names and missing attestations before creating a draft release
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence: [test, contract-test]
    security_review_complete:
      required_evidence: [security-review]
    health_checks_green:
      required_evidence: [runtime-smoke]
  reconciliation:
    required_evidence: [governance-validate, governance-exposure-scan]
  finding_registry: governance/findings.yml
