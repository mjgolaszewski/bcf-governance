document:
  kind: hotfix_execution_log
  name: P10-HF02 Hotfix Log
  id: bcf-governance-phase-10-hotfix02
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-10-hotfix02.yml
hotfix:
  id: P10-HF02
  mode: full
  related_phase_id: P10
  hotfix_number: 2
  summary: Produce the trusted BCF controller and exact-main bootstrap artifact
execution_evidence:
  planned_commands:
  - python3 -m pytest tests/test_ci_github_reference.py tests/test_ci_authority.py tests/test_ci_authority_decisions.py
  executed_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  - offline wheelhouse install followed by bcf ci-github --help
  notes:
  - P10-HF01 exact-main probe run 33337985186 proved that neither trusted runner has BCF installed
  - this layer implements the trusted control commands and produces an exact-main wheel; later hotfix layers pin and deploy those bytes before activation
  - 376 exact tests pass, including 104 CI-authority contract nodes; both high-value mutant profiles pass
  - trusted run, workflow, repository, path, workflow bytes, event, commit, tree, run attempt, producer jobs, and publication precedence fail closed under focused adversarial tests
  - a clean Python environment installs the controller and all hash-manifested dependencies from the offline wheelhouse and launches the trusted CLI without network access
  - callbacks return a clean pending result without partial artifacts when any producer has not started or is still running; no polling, sleeping, or occupied waiter is introduced
  - status publication and all authority callbacks remain disabled until the exact-main wheelhouse is pinned, installed, and re-probed on both trusted runners
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence: [test, contract-test]
    security_review_complete:
      required_evidence: [security-review]
    health_checks_green:
      required_evidence: [runtime-smoke]
  reconciliation:
    required_evidence: [governance-validate, governance-exposure-scan]
  finding_registry: governance/findings.yml
