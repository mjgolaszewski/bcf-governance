document:
  kind: hotfix_execution_log
  name: P13-HF05 Hotfix Log
  id: project-phase-13-hotfix05
  version: 1.0.0
  generated_at_utc: '2026-09-01T00:00:00Z'
  status: completed
  path: phases/phase-13-hotfix05.yml
hotfix:
  id: P13-HF05
  mode: full
  related_phase_id: P13
  hotfix_number: 5
  summary: Rotate the mechanically resolved publication controller onto both trusted
    runners
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/preflight_governance.py --repo-root . --mode release
    --python .venv/bin/python --format text
  executed_commands:
  - command: exact-main admission run 33522183544 attempt 1
    result: pass
    notes: merge 49272182790342a52ef7ddbcfd6a540ca7df6baf passed every producer and emitted controller artifact 9806282487
  - command: bcf ci-github controller-pin resolve and compile
    result: pass
    notes: provider APIs and downloaded bytes derived run, attempt, artifact, digest, wheel hash, repository, commit, and tree without operator transcription
  - command: bcf ci sync-self-controller --pin compiled-pin.json --apply
    result: pass
    notes: the canonical projector updated exactly the policy, bootstrap, probe, and verifier target surfaces while retaining the prior installed controller
  notes:
  - target controller commit is 49272182790342a52ef7ddbcfd6a540ca7df6baf,
    tree cd714d075fb0b2271efe5416fb83191ccd18259c, and wheel SHA-256
    9e17a9fd84df251bcbfe7509376d1e3fc531443fcb87458bd1c114559dea58ff
  - bootstrap still invokes installed controller de2eeee6 until provider proofs show the
    new controller exists independently on both uniquely labeled trusted runners
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
