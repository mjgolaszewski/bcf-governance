document:
  kind: hotfix_execution_log
  name: P10-HF03 Hotfix Log
  id: bcf-governance-phase-10-hotfix03
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-10-hotfix03.yml
hotfix:
  id: P10-HF03
  mode: full
  related_phase_id: P10
  hotfix_number: 3
  summary: Stage exact-main trusted controller provisioning for both isolated control runners
execution_evidence:
  planned_commands:
  - python3 -m pytest -q tests/test_self_governance_contracts.py tests/test_ci_github_controller.py
  - python3 scripts/validate_governance_yaml.py --repo-root .
  - owner-dispatch bcf/trusted-control-bootstrap and bcf/trusted-control-probe from exact main
  executed_commands:
  - .venv/bin/python -m pytest -q tests
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile high-value
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-high-value
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root . --format json
  - .venv/bin/python scripts/check_governance_exposure.py --repo-root . --format json
  - bash syntax validation for every trusted bootstrap and probe run block
  notes:
  - HF02 merge commit 6368eb34ce08c1bfc4bc554fae02e5e10468455f is certified by governance run 33339477453 and pack run 33339477463
  - exact artifact 9740097978 has provider digest sha256:b6015abf6b6b761c1f909d6e66fe72e71c0f67b84066db907800ab3b6c27d115 and verified internal wheel hashes
  - repository runners 22 and 21 have additive unique labels bcf-trusted-control-1 and bcf-trusted-control-2 respectively
  - bootstrap remains owner-main-only, checks out no repository, authenticates the provider artifact before download, and installs offline under the runner tool cache
  - callbacks, status authority, and release publication remain disabled until both independently addressed runners pass the post-install probe
  - 377 exact tests and 105 contract nodes pass; both high-value mutant profiles, structural validation, exposure scan, and trusted shell syntax pass
  - the artifact-ID semantic owner has a cause-verified negative control in the canonical contract gate
  - this completed layer stages the only permitted bootstrap workflow; actual installation is a post-merge exact-main owner dispatch and is not claimed by this authored record
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence: [test, contract-test]
    security_review_complete:
      required_evidence: [security-review]
    health_checks_green:
      required_evidence: [runtime-smoke]
  reconciliation:
    required_evidence: [governance-validate, governance-exposure-scan]
  finding_registry: governance/findings.yml
