document:
  kind: hotfix_execution_log
  name: P02-HF01 Hotfix Log
  id: bcf-governance-phase-02-hotfix01
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-02-hotfix01.yml
hotfix:
  id: P02-HF01
  mode: full
  related_phase_id: P02
  hotfix_number: 1
  summary: route unsafe pull requests to disposable hosted workers and trusted contributor CI to the local pool
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  executed_commands:
  - .venv/bin/python -m pytest tests/test_self_governance_contracts.py tests/test_validate_governance_yaml.py -q
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  - .venv/bin/python -m pytest tests
  notes:
  - GitHub-hosted capacity resumed before the prior local-window expiration; fork or non-trusted pull requests now use one fresh provider-managed ubuntu-latest VM per job
  - owner-authored same-repository PRs, protected main, schedules, and tags use the repository-scoped bcf-governance local pool
  - the routing expression is mechanically tested so unsafe code cannot select a persistent local runner and hosted capacity is not a fallback for trusted work
  - no workflow run command polls, sleeps, waits, or loops for another workflow or runner
  - evidence gates are derived from canonical contracts and partitioned exactly once across four run-to-completion shards rather than allocating 21 setup-heavy jobs
  - release execution remains disabled until P11 and trusted publication remains disabled until P10 isolation proof
  - local validation passed governance-yaml-ok and the complete 248-test suite
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
