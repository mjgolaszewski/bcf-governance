document:
  kind: hotfix_execution_log
  name: P02-HF01 Hotfix Log
  id: bcf-governance-phase-02-hotfix01
  version: 1.0.0
  generated_at_utc: '2026-08-30T00:00:00Z'
  status: completed
  path: phases/phase-02-hotfix01.yml
hotfix:
  id: P02-HF01
  mode: full
  related_phase_id: P02
  hotfix_number: 1
  summary: route BCF workflows to the owner-only local VM runner pool and protect
    main while hosted credits are unavailable
execution_evidence:
  planned_commands:
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  - .venv/bin/python -m pytest tests
  executed_commands:
  - gh api repos/mjgolaszewski/bcf-governance/actions/runners
  - gh api repos/mjgolaszewski/bcf-governance/collaborators?affiliation=all
  - gh api --method POST repos/mjgolaszewski/bcf-governance/rulesets
  - .venv/bin/python -m pytest -q tests/test_self_governance_contracts.py
  - .venv/bin/python -m pytest tests -q
  - .venv/bin/python scripts/validate_governance_yaml.py --repo-root .
  notes:
  - the live runner label is vm-linux-ci-runner, not vm-ci-runner
  - two matching local runners were online and idle; the Dependabot-only runner is excluded
  - owner and same-repository PR admission is evaluated before runner assignment
  - repository ruleset 21862678 protects main with no bypass actors and required current checks
  - GitHub personal repositories reject explicit user push restrictions, but mjgolaszewski is the sole collaborator and the no-bypass pull-request rule applies to administrators
  - privileged release publication is disabled while trusted and candidate labels share the local pool
  - local validation passed 198 tests and governance-yaml-ok
  - restore ephemeral candidate workers and isolated trusted publication before the temporary window expires
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
