document:
  kind: hotfix_execution_log
  name: P19-HF02 Hotfix Log
  id: project-phase-19-hotfix02
  version: 1.0.0
  generated_at_utc: '2026-09-10T00:00:00Z'
  status: completed
  path: phases/phase-19-hotfix02.yml
hotfix:
  id: P19-HF02
  mode: full
  related_phase_id: P19
  hotfix_number: 2
  summary: Release source-test environment authority and failure-evidence retention
execution_evidence:
  planned_commands:
  - .venv/bin/python -m pytest -q tests
  executed_commands:
  - .venv/bin/python -m pytest -q tests/test_release_workflow_contracts.py tests/test_ci_graph.py tests/test_profile_contract_v2.py tests/test_preflight.py tests/test_ci_github_release.py (121 passed)
  - .venv/bin/python -m pytest -q tests after mechanical workflow-authority pinning (762 passed)
  - .venv/bin/python .github/scripts/run_self_governance_gate.py lint (pass)
  - .venv/bin/python .github/scripts/run_self_governance_gate.py typecheck (pass)
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile full (15 of 15 killed)
  - .venv/bin/python .github/scripts/run_validator_mutants.py --profile semantic-full (12 of 12 killed)
  - make -f Makefile.fragment release-check PYTHON=.venv/bin/python BCF_EVIDENCE_DIR=.artifacts/p19-hf02-release-check (non-authoritative local evidence; 21 of 21 receipts, 197 of 197 controls, state closed)
  - clean CPython 3.12.14 release builder on commit 905283bca463066e921cc1a189b0ea5058cc3f64 (762 source tests, wheel, sdist, checksums, and three-asset build manifest passed)
  - independent offline controller verifier (wheel smoke and Twine passed; extracted sdist 759 passed and 3 declared Git-custody tests skipped)
  - corrective PR 141, exact-main 34430038100, and scheduled mutants 34430476314 and 34430476699 passed on attempt 1
  - release build 34430626218 and independent verifier 34430961757 passed on attempt 1; trusted collector 34431293005 emitted receipt artifact 10134580860
  - immutable publisher 34431431532 published only the receipt-bound v1.0.3 assets on attempt 1
  notes:
  - release run 34422503200 attempt 1 failed on exact completed main c3cf96de9a15b17342f46be66cc68033b01ff34d because source tests launched from the release builder did not receive exact checkout import authority
  - the same failed attempt discarded its raw pytest and JUnit files because the build artifact upload was success-only; the hosted run is the sole diagnostic sample and is not retried
  - source tests now receive an exact mechanically derived repository-root PYTHONPATH, failed subprocess output is closed, preserved, and replayed, and the same-attempt build bundle uploads on failure
  - the verifier remains success-gated on the complete release workflow conclusion, so partial diagnostic artifacts cannot become release inputs
  - focused release, graph, profile, preflight, and provider-authority tests passed locally before full validation
  - the first complete suite pass intentionally collected two expected workflow-authority failures after generated bytes changed; workflow commit b6b014600db10668530bfb126b6265ad011eda11 was then frozen and all authority registry values were compiled from its exact Git bytes before the 762-test suite passed
  - clean-room Docker setup required only local bind-mount Git safe-directory declarations and the provider-declared pre-existing GITHUB_OUTPUT file; neither altered repository behavior or release bytes
  - the corrected remote release build completed in one attempt and retained its exact bundle; no hosted retry or check-fix loop occurred
  - audits/v1.0.3-release-custody.yml owns the tag, release, asset, receipt, attestation, run, runner, and credential-removal evidence
closeout_requirements:
  claims:
    required_suites_green:
      required_evidence:
      - test
      - contract-test
    security_review_complete:
      required_evidence:
      - security-review
    health_checks_green:
      required_evidence:
      - runtime-smoke
  reconciliation:
    required_evidence:
    - governance-validate
    - governance-exposure-scan
  finding_registry: governance/findings.yml
