document:
  kind: template_governance_pack_manifest
  name: BCF Template And Governance Pack
  id: bcf-governance
  version: 0.6.1
  generated_at_utc: "2026-08-14T00:00:00Z"
  status: tracked_inventory
source_model:
  origin: public reusable governance pack
  governance_sources:
    - README.md
    - docs/USAGE.md
    - docs/MAINTAINING.md
    - template-repo/AGENTS.yml
    - template-repo/governance/artifact-manifest.yml
    - template-repo/governance/evidence-policy.yml
    - template-repo/governance/gate-contracts.yml
    - template-repo/docs/OPERATIONS.md
pack_contents:
  template_repo:
    - root governance templates
    - profile and release-gate classification template
    - configurable architecture boundary rule template
    - mandatory structural anti-drift gate configuration
    - artifact root manifest with audits, vendored packs, line and KiB context budgets, and nested-governance rules
    - repo cleanup contract with deterministic actions, semantic review requirements, and closeout rules
    - governed artifact structural schemas
    - observability telemetry and logging contract templates
    - existing-repo adoption playbooks in human and machine-readable forms
    - explicit governance dependency requirements
    - phase planning templates
    - governed phase-numbered hotfix log template
    - typed evidence, finding registry, and computed lifecycle contracts
    - operations template
    - Makefile target fragment
    - GitHub Actions governance workflow template
    - CQRS-lite architecture test skeleton
  documentation:
    - concise project overview and lifecycle contract
    - canonical install, adoption, evidence, and cleanup guide
    - canonical maintainer and release guide
    - installed repository operations runbook
  scripts:
    - installer helper that copies the template pack, replaces placeholders, applies profiles, opens the first phase, and reports release-gate wiring gaps
    - installer existing-repo adoption mode for conversion and inventory phases
    - doctor helper that reports remaining placeholder and release-gate wiring gaps
    - transactional cleanup helper that validates a shadow worktree before atomic replacement and rollback-safe transfer
    - exact-label Docker CI resource cleanup helper with dry-run and explicit apply modes
    - opt-in redacted publication audit across unique blobs reachable from complete local Git history
    - generic phase and hotfix scaffold helper that emits repo-relative document paths
    - portable evidence capture and truthfulness helpers with exact-tree binding
    - idempotent evidence-integrity migration helper
    - generic governance YAML validator with structural and semantic checks, compact json output, release-gate placeholder detection, path portability checks, governance drift guardrails, repo cleanup contract validation, line and KiB context budgets, and instantiated placeholder detection
    - tag-triggered release workflow for tested, attested wheel, sdist, and checksum assets
tracking:
  intended_location: manifest.yml
  tracked_by_git: true
  generated_artifact_ignore_rule: .artifacts/
